import React, { Component } from 'react';
import { Keyboard } from '../Keyboard';

class App extends Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <Keyboard />
    );
  }
}

export default App;
